		var href = document.getElementById('redirection');

//RESULT//

		if (result.value === "2d" && line.value === "2l"){ // Albi ET Albi - St-Sulpice //
		var redirec = "possible";
			}

		if(result.value == "2d" && line.value == "4l") // Albi ET Albi - Castres //
		var redirec = "possible";
			}
			
		if(result.value == "3d" && line.value == "2l") // Gaillac ET Albi - St-Sulpice //
		var redirec = "possible";
			}
			
		if(result.value == "3d" && line.value == "3l") // Gaillac ET St-Sulpice - Albi //
		var redirec = "possible";
			}
			
		if(result.value == "4d" && line.value == "3l") // St-Sulpice ET St-Sulpice - Albi //
		var redirec = "possible";
			}
			
		if(result.value == "5d" && line.value == "4l") // Réalmont ET Albi - Castres //
		var redirec = "possible";
			}
			
		if(result.value == "5d" && line.value == "5l") // Réalmont ET Castres - Albi //
		var redirec = "possible";
			}
		
		if(result.value == "6d" && line.value == "5l") // Castres ET Castres - Albi //
		var redirec = "possible";
			}
			
//IMPOSSIBLE//

		if(result.value == "6d" && line.value == "4l") // Castres ET Albi - Castres //
		var redirec = "impossible";
			}

		if(result.value == "6d" && line.value == "3l") // Castres ET St-Sulpice - Albi //
		var redirec = "impossible";
			}
			
		if(result.value == "6d" && line.value == "2l") // Castres ET Albi - St-Sulpice //
		var redirec = "impossible";
			}

		if(result.value == "5d" && line.value == "3l") // Réalmont ET St-Sulpice - Albi //
		var redirec = "impossible";
			}

		if(result.value == "5d" && line.value == "2l") // Réalmont ET Albi - St-Sulpice //
		var redirec = "impossible";
			}

		if(result.value == "4d" && line.value == "5l") // St-Sulpice ET Castres - Albi //
		var redirec = "impossible";
			}

		if(result.value == "4d" && line.value == "4l") // St-Sulpice ET Albi - Castres //
		var redirec = "impossible";
			}

		if(result.value == "4d" && line.value == "2l") // St-Sulpice ET Albi - St-Sulpice //
		var redirec = "impossible";
			}

		if(result.value == "3d" && line.value == "5l") // Gaillac ET Castres - Albi //
		var redirec = "impossible";
			}

		if(result.value == "3d" && line.value == "4l") // Gaillac ET Albi - Castres //
		var redirec = "impossible";
			}

		if(result.value == "2d" && line.value == "3l") // Albi ET Castres - Albi //
		var redirec = "impossible";
			}

		if(result.value == "2d" && line.value == "5l") // Albi ET St-Sulpice - Albi //
		var redirec = "impossible";
			}